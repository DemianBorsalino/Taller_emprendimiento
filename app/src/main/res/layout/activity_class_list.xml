<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:title="Clases disponibles"
        android:background="?attr/colorPrimaryContainer"
        android:titleTextColor="?attr/colorOnPrimaryContainer"
        app:menu="@menu/menu_topbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvClasses"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabCreateClass"
        android:src="@android:drawable/ic_input_add"
        android:contentDescription="Crear nueva clase"
        app:layout_anchorGravity="bottom|end"
        android:layout_margin="16dp"
        app:layout_anchor="@id/rvClasses"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
